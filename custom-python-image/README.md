# Custom Python Image

This module will define the custom Python auto-instrumentation image used by the PoC, including its container build, packaging, and integration points with the runtime coordinator.
