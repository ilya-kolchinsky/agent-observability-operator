# Runtime Coordinator

This module will contain the runtime coordinator process that runs inside the custom Python auto-instrumentation image and manages startup, instrumentation hooks, and agent process orchestration.
