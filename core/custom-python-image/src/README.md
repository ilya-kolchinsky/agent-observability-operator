This directory contains Python files copied directly into the custom Python image.

Current contents:

- `sitecustomize.py` - imported automatically by Python startup so the runtime coordinator can run early without replacing the application's normal entrypoint or command.
